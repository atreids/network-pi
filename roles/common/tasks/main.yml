- name: Update and upgrade apt packages
  ansible.builtin.apt:
    update_cache: yes
    upgrade: dist
    autoremove: yes
    autoclean: yes

- name: Install common packages
  ansible.builtin.apt:
    name:
      - vim
      - curl
      - wget
      - htop
      - git
    state: present